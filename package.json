{
    "name": "obsidian-term",
    "version": "1.0.0",
    "description": "Obsidian Terminal Plugin with @ autocomplete",
    "main": "main.js",
    "scripts": {
        "build:helper": "cargo build --release --manifest-path native/pty-helper/Cargo.toml && mkdir -p resources && cp native/pty-helper/target/release/pty-helper resources/pty-helper && chmod +x resources/pty-helper",
        "dev": "node esbuild.config.mjs",
        "build": "npm run build:helper && node esbuild.config.mjs production",
        "release:github": "./scripts/release-github.sh"
    },
    "keywords": [
        "obsidian",
        "terminal",
        "xterm"
    ],
    "license": "MIT",
    "devDependencies": {
        "@types/node": "^20.10.0",
        "builtin-modules": "^3.3.0",
        "esbuild": "^0.19.0",
        "obsidian": "latest",
        "typescript": "^5.3.0"
    },
    "dependencies": {
        "xterm": "^5.3.0",
        "xterm-addon-fit": "^0.8.0"
    }
}